let nextId = 1;

export const topNavItems = [
    { id: `${nextId++}`, title: "Home", to: "/" },
    { id: `${nextId++}`, title: "About", to: "/about" },
    { id: `${nextId++}`, title: "Services", to: "/services" },
    { id: `${nextId++}`, title: "Contact", to: "/contact" },
];
